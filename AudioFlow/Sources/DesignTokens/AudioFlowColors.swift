//
//  AudioFlowColors.generated.swift
//  Generated from design-tokens/tokens.yaml. Do not edit manually.
//

import SwiftUI

extension Color {
    static let primary500 = Color(tokenHex: "007AFF")
    static let primary600 = Color(tokenHex: "0056CC")
    static let primary700 = Color(tokenHex: "0044A8")
    static let darkBackground = Color(tokenHex: "000000")
    static let darkSurface = Color(tokenHex: "1C1C1E")
    static let darkElevated = Color(tokenHex: "2C2C2E")
    static let lightBackground = Color(tokenHex: "F9FAFB")
    static let lightSurface = Color(tokenHex: "FFFFFF")
    static let lightElevated = Color(tokenHex: "FFFFFF")
    static let darkTextPrimary = Color(tokenHex: "FFFFFF")
    static let darkTextSecondary = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.80)
    static let darkTextTertiary = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.60)
    static let darkTextDisabled = Color(tokenHex: "8E8E93")
    static let darkTextLabel = Color(tokenHex: "999999")
    static let lightTextPrimary = Color(tokenHex: "111827")
    static let lightTextSecondary = Color(tokenHex: "6B7280")
    static let lightTextTertiary = Color(tokenHex: "9CA3AF")
    static let lightTextDisabled = Color(tokenHex: "D1D5DB")
    static let lightTextLabel = Color(tokenHex: "9CA3AF")
    static let darkBorderSubtle = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.05)
    static let darkBorderDefault = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.10)
    static let darkBorderStrong = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.20)
    static let lightBorderSubtle = Color(.sRGB, red: 0 / 255, green: 0 / 255, blue: 0 / 255, opacity: 0.06)
    static let lightBorderDefault = Color(.sRGB, red: 0 / 255, green: 0 / 255, blue: 0 / 255, opacity: 0.10)
    static let lightBorderStrong = Color(.sRGB, red: 0 / 255, green: 0 / 255, blue: 0 / 255, opacity: 0.15)
    static let rose500 = Color(tokenHex: "F43F5E")
    static let rose600 = Color(tokenHex: "E11D48")
    static let green500 = Color(tokenHex: "10B981")
    static let green600 = Color(tokenHex: "059669")
    static let amber500 = Color(tokenHex: "F59E0B")
    static let amber600 = Color(tokenHex: "D97706")
    static let backgroundPrimaryDark = Color(tokenHex: "000000")
    static let backgroundSurfaceDark = Color(tokenHex: "1C1C1E")
    static let backgroundElevatedDark = Color(tokenHex: "2C2C2E")
    static let backgroundPrimaryLight = Color(tokenHex: "F9FAFB")
    static let backgroundSurfaceLight = Color(tokenHex: "FFFFFF")
    static let backgroundElevatedLight = Color(tokenHex: "FFFFFF")
    static let textPrimaryDark = Color(tokenHex: "FFFFFF")
    static let textSecondaryDark = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.80)
    static let textTertiaryDark = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.60)
    static let textDisabledDark = Color(tokenHex: "8E8E93")
    static let textLabelDark = Color(tokenHex: "999999")
    static let textPrimaryLight = Color(tokenHex: "111827")
    static let textSecondaryLight = Color(tokenHex: "6B7280")
    static let textTertiaryLight = Color(tokenHex: "9CA3AF")
    static let textDisabledLight = Color(tokenHex: "D1D5DB")
    static let textLabelLight = Color(tokenHex: "9CA3AF")
    static let interactivePrimary = Color(tokenHex: "007AFF")
    static let interactivePrimaryHover = Color(tokenHex: "0056CC")
    static let interactivePrimaryActive = Color(tokenHex: "0044A8")
    static let interactivePrimaryBackground = Color(.sRGB, red: 0 / 255, green: 122 / 255, blue: 255 / 255, opacity: 0.10)
    static let interactivePrimaryBorder = Color(.sRGB, red: 0 / 255, green: 122 / 255, blue: 255 / 255, opacity: 0.20)
    static let borderSubtleDark = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.05)
    static let borderDefaultDark = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.10)
    static let borderStrongDark = Color(.sRGB, red: 255 / 255, green: 255 / 255, blue: 255 / 255, opacity: 0.20)
    static let borderSubtleLight = Color(.sRGB, red: 0 / 255, green: 0 / 255, blue: 0 / 255, opacity: 0.06)
    static let borderDefaultLight = Color(.sRGB, red: 0 / 255, green: 0 / 255, blue: 0 / 255, opacity: 0.10)
    static let borderStrongLight = Color(.sRGB, red: 0 / 255, green: 0 / 255, blue: 0 / 255, opacity: 0.15)
    static let destructive = Color(tokenHex: "F43F5E")
    static let destructiveHover = Color(tokenHex: "E11D48")
    static let destructiveBackground = Color(.sRGB, red: 244 / 255, green: 63 / 255, blue: 94 / 255, opacity: 0.10)
    static let success = Color(tokenHex: "10B981")
    static let successHover = Color(tokenHex: "059669")
    static let successBackground = Color(.sRGB, red: 16 / 255, green: 185 / 255, blue: 129 / 255, opacity: 0.10)
    static let warning = Color(tokenHex: "F59E0B")
    static let warningHover = Color(tokenHex: "D97706")
    static let warningBackground = Color(.sRGB, red: 245 / 255, green: 158 / 255, blue: 11 / 255, opacity: 0.10)

    // Compatibility aliases used by the current SwiftUI component layer.
    static let primaryHover = primary600
    static let primaryActive = primary700
    static let backgroundDark = darkBackground
    static let surfaceDark = darkSurface
    static let surfaceElevated = darkElevated
    static let textPrimary = darkTextPrimary
    static let textSecondary = darkTextSecondary
    static let textTertiary = darkTextTertiary
    static let textDisabled = darkTextDisabled
    static let textLabel = darkTextLabel
    static let borderSubtle = darkBorderSubtle
    static let borderDefault = darkBorderDefault
    static let borderStrong = darkBorderStrong
    static let destructiveDefault = destructive
}

extension Color {
    init(tokenHex: String) {
        let hex = tokenHex.trimmingCharacters(in: CharacterSet.alphanumerics.inverted)
        var int: UInt64 = 0
        Scanner(string: hex).scanHexInt64(&int)
        let a, r, g, b: UInt64

        switch hex.count {
        case 3:
            (a, r, g, b) = (255, (int >> 8) * 17, (int >> 4 & 0xF) * 17, (int & 0xF) * 17)
        case 6:
            (a, r, g, b) = (255, int >> 16, int >> 8 & 0xFF, int & 0xFF)
        case 8:
            (a, r, g, b) = (int >> 24, int >> 16 & 0xFF, int >> 8 & 0xFF, int & 0xFF)
        default:
            (a, r, g, b) = (255, 0, 0, 0)
        }

        self.init(
            .sRGB,
            red: Double(r) / 255,
            green: Double(g) / 255,
            blue: Double(b) / 255,
            opacity: Double(a) / 255
        )
    }
}
