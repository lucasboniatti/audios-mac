# Story 2.2: Implementar estados do app (IDLE, RECORDING, PROCESSING)

**Epic:** EPIC-001 (AudioFlow)  
**Status:** Draft  
**Priority:** MUST  
**Effort:** M (Medium)  
**Phase:** 2 - Menu Bar App Base  

---

## Story

**Como** usuário  
**Eu quero** ver indicadores visuais do estado atual do app  
**Para que** eu saiba se está gravando, processando ou pronto  

---

## Acceptance Criteria

- [ ] AppState define 3 estados: IDLE, RECORDING, PROCESSING
- [ ] Ícone muda conforme estado (mic.fill → red mic → spinner)
- [ ] Menu atualiza conforme estado
- [ ] Transições de estado são validadas

---

## Technical Details

### State Machine

```
┌───────────┐    Space     ┌───────────┐    Space     ┌───────────┐
│   IDLE    │─────────────▶│ RECORDING │─────────────▶│PROCESSING │
└───────────┘              └───────────┘              └─────┬─────┘
     ▲                                                      │
     │                    Transcrição completa              │
     └──────────────────────────────────────────────────────┘
```

### Files to Create/Update

- `Models/AppState.swift` (novo)
- `Views/MenuBuilder.swift` (atualizar)
- `AppDelegate.swift` (atualizar)

### AppState.swift

```swift
import Foundation
import Combine

enum AppState: Equatable {
    case idle
    case recording
    case processing(partialText: String)
}

class AppStateManager: ObservableObject {
    @Published var state: AppState = .idle
    
    func transition(to newState: AppState) {
        // Validate transitions
        switch (state, newState) {
        case (.idle, .recording),
             (.recording, .processing),
             (.processing, .idle):
            state = newState
        default:
            print("Invalid transition: \(state) -> \(newState)")
        }
    }
}
```

### Ícones por Estado

| Estado | SF Symbol | Cor |
|--------|-----------|-----|
| IDLE | mic.fill | padrão |
| RECORDING | mic.fill | vermelho |
| PROCESSING | arrow.clockwise | animado |

### Menu por Estado

**IDLE:**
```
🎤 AudioFlow
├── Gravar (Space)
├── ───────────────
├── Sobre AudioFlow
└── Sair
```

**RECORDING:**
```
🔴 Gravando...
├── Parar (Space)
├── ───────────────
├── [texto parcial em tempo real...]
└── Cancelar
```

**PROCESSING:**
```
⏳ Processando...
├── [texto parcial...]
└── Cancelar
```

---

## Dev Notes

- Usar `ObservableObject` para reatividade
- Ícone vermelho durante gravação: `button.image?.isTemplate = false` + tint
- Processamento pode mostrar partial results

---

## Definition of Done

- [ ] 3 estados implementados
- [ ] Ícone muda visualmente entre estados
- [ ] Menu mostra opções diferentes por estado
- [ ] Transições funcionam corretamente

---

*Story created by @pm (Morgan)*
