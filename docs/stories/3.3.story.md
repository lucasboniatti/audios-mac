# Story 3.3: Implementar transcrição com SFSpeechRecognizer

**Epic:** EPIC-001 (AudioFlow)  
**Status:** Draft  
**Priority:** MUST  
**Effort:** M (Medium)  
**Phase:** 3 - Core Features - Audio & Speech  

---

## Story

**Como** usuário  
**Eu quero** que meu áudio seja transcrito em português brasileiro  
**Para que** eu tenha o texto da minha fala  

---

## Acceptance Criteria

- [ ] Transcrição funciona em pt-BR
- [ ] Partial results atualizam em tempo real durante gravação
- [ ] Transcrição final é retornada ao completar
- [ ] Erro tratado se Speech Recognition indisponível
- [ ] Erro tratado se permissão negada

---

## Technical Details

### Key APIs

- `SFSpeechRecognizer`
- `SFSpeechAudioBufferRecognitionRequest`
- `SFSpeechRecognitionTask`

### Permissions

```xml
<key>NSSpeechRecognitionUsageDescription</key>
<string>AudioFlow precisa de acesso ao reconhecimento de voz para transcrever áudio</string>
```

### Files to Create

- `Services/SpeechService.swift`

### SpeechService.swift

```swift
import Speech
import AVFoundation

class SpeechService: NSObject {
    private let speechRecognizer = SFSpeechRecognizer(locale: Locale(identifier: "pt_BR"))
    private var recognitionRequest: SFSpeechAudioBufferRecognitionRequest?
    private var recognitionTask: SFSpeechRecognitionTask?
    
    var onPartialResult: ((String) -> Void)?
    var onFinalResult: ((String) -> Void)?
    var onError: ((Error) -> Void)?
    
    func requestAuthorization(completion: @escaping (Bool) -> Void) {
        SFSpeechRecognizer.requestAuthorization { status in
            DispatchQueue.main.async {
                completion(status == .authorized)
            }
        }
    }
    
    func startRecognition() throws {
        recognitionRequest = SFSpeechAudioBufferRecognitionRequest()
        recognitionRequest?.shouldReportPartialResults = true
        
        recognitionTask = speechRecognizer?.recognitionTask(with: recognitionRequest!) { [weak self] result, error in
            if let result = result {
                let text = result.bestTranscription.formattedString
                self?.onPartialResult?(text)
                
                if result.isFinal {
                    self?.onFinalResult?(text)
                }
            }
            
            if let error = error {
                self?.onError?(error)
            }
        }
    }
    
    func appendAudioBuffer(_ buffer: AVAudioPCMBuffer) {
        recognitionRequest?.append(buffer)
    }
    
    func stopRecognition() {
        recognitionRequest?.endAudio()
        recognitionTask?.cancel()
        recognitionTask = nil
        recognitionRequest = nil
    }
}
```

---

## Error Handling

| Error | Behavior |
|-------|----------|
| Speech Recognition indisponível | Mostrar mensagem no menu |
| Permissão negada | Alert com link para System Preferences |
| Erro de rede | Continuar tentando, mostrar indicador |
| No speech detected | Retornar string vazia |

---

## Dev Notes

- `shouldReportPartialResults = true` para real-time feedback
- `Locale(identifier: "pt_BR")` para português brasileiro
- SFSpeechRecognizer pode requerer internet para alguns casos
- Testar offline behavior

---

## Definition of Done

- [ ] Transcrição funciona em pt-BR
- [ ] Partial results aparecem em tempo real
- [ ] Final result é retornado
- [ ] Erros tratados adequadamente

---

*Story created by @pm (Morgan)*
