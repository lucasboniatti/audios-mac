# Story 1.1: Criar projeto Xcode com estrutura base

**Epic:** EPIC-001 (AudioFlow)  
**Status:** Draft  
**Priority:** MUST  
**Effort:** S (Small)  
**Phase:** 1 - Project Setup & Scaffolding  

---

## Story

**Como** desenvolvedor  
**Eu quero** um projeto Xcode inicializado com estrutura de pastas  
**Para que** eu possa começar a implementar as features do AudioFlow  

---

## Acceptance Criteria

- [ ] Projeto Xcode compila sem erros
- [ ] Estrutura de pastas criada conforme spec (App/, Controllers/, Models/, Views/, Services/, Resources/)
- [ ] Info.plist com permissões configuradas (NSMicrophoneUsageDescription, NSSpeechRecognitionUsageDescription)
- [ ] Target macOS 12.0 configurado
- [ ] AppDelegate.swift criado com template básico

---

## Technical Details

### Files to Create

```
AudioFlow/
├── AudioFlow.xcodeproj
├── App/
│   ├── AppDelegate.swift
│   └── Info.plist
├── Controllers/
│   └── .gitkeep
├── Models/
│   └── .gitkeep
├── Views/
│   └── .gitkeep
├── Services/
│   └── .gitkeep
└── Resources/
    └── .gitkeep
```

### Info.plist Keys

```xml
<key>NSMicrophoneUsageDescription</key>
<string>AudioFlow precisa de acesso ao microfone para transcrever sua voz</string>
<key>NSSpeechRecognitionUsageDescription</key>
<string>AudioFlow precisa de acesso ao reconhecimento de voz para transcrever áudio</string>
<key>LSUIElement</key>
<true/>
```

### AppDelegate Template

```swift
import Cocoa

@main
class AppDelegate: NSObject, NSApplicationDelegate {
    func applicationDidFinishLaunching(_ aNotification: Notification) {
        // Setup will go here
    }
    
    func applicationWillTerminate(_ aNotification: Notification) {
        // Cleanup will go here
    }
}
```

---

## Dev Notes

- `LSUIElement = true` faz o app aparecer apenas no menu bar (sem ícone no Dock)
- Usar Swift 5.9 e macOS 12.0 como deployment target
- Não usar SwiftUI, apenas AppKit

---

## Definition of Done

- [ ] Código commitado
- [ ] Projeto abre no Xcode sem erros
- [ ] Build funciona (Cmd+B)
- [ ] App roda e mostra "Hello World" no console

---

*Story created by @pm (Morgan)*
