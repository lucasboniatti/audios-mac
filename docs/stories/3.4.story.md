# Story 3.4: Implementar clipboard automático

**Epic:** EPIC-001 (AudioFlow)  
**Status:** In Progress  
**Priority:** MUST  
**Effort:** S (Small)  
**Phase:** 3 - Core Features - Audio & Speech  

---

## Story

**Como** usuário  
**Eu quero** que o texto transcrito vá automaticamente para o clipboard  
**Para que** eu possa colar imediatamente com Cmd+V  

---

## Acceptance Criteria

- [ ] Texto transcrito vai para clipboard automaticamente
- [ ] Cmd+V funciona em qualquer app após transcrição
- [ ] Clipboard é limpo antes de copiar novo texto
- [ ] Notificação "Texto copiado" mostrada ao usuário

---

## Technical Details

### Key APIs

- `NSPasteboard.general`

### Files to Create

- `Services/ClipboardService.swift`

### ClipboardService.swift

```swift
import Cocoa

class ClipboardService {
    static let shared = ClipboardService()
    
    func copyToClipboard(_ text: String) {
        let pasteboard = NSPasteboard.general
        pasteboard.clearContents()
        pasteboard.setString(text, forType: .string)
        
        // Post notification
        NotificationCenter.default.post(name: .textCopied, object: nil)
    }
    
    func getClipboardText() -> String? {
        return NSPasteboard.general.string(forType: .string)
    }
}

extension Notification.Name {
    static let textCopied = Notification.Name("textCopied")
}
```

---

## Integration

```swift
// No AppDelegate ou Controller
speechService.onFinalResult = { [weak self] text in
    ClipboardService.shared.copyToClipboard(text)
    self?.showNotification("Texto copiado para o clipboard")
}
```

---

## Dev Notes

- Usar `clearContents()` antes de `setString()` para garantir clipboard limpo
- Notificação pode ser um simples alert ou NSUserNotification

---

## Definition of Done

- [ ] Texto vai para clipboard após transcrição
- [ ] Cmd+V funciona em outros apps
- [ ] Notificação mostrada
- [ ] Clipboard limpo entre transcrições

## Regression Fix Checklist

- [x] Texto final continua sendo a ultima coisa copiada para o clipboard
- [x] Auto-paste usa simulacao explicita de `Cmd+V` via `CGEvent`
- [x] Falha silenciosa por falta de Accessibility virou prompt/alerta claro
- [x] Fluxo de auto-paste coberto com testes unitarios
- [x] Alerta de Accessibility nao reaparece a cada gravacao dentro da mesma sessao
- [x] Falso positivo de Accessibility nao interrompe mais a cola automatica com alerta modal
- [x] Fallback via AppleScript/System Events restaura a cola automatica quando a trust-check de Accessibility falha

## File List

| File | Action |
|------|--------|
| `AudioFlow/Sources/AppDelegate.swift` | Modified - delegated auto-paste to a dedicated service after clipboard commit |
| `AudioFlow/Sources/AutoPasteService.swift` | Modified - falls back to `System Events` paste when Accessibility trust cannot be confirmed and avoids the false-positive modal |
| `AudioFlow/Tests/AutoPasteServiceTests.swift` | Modified - verifies the Accessibility fallback path and primary paste path both remain covered |
| `build_app.sh` | Modified - ad-hoc signs the generated `.app` with the stable identifier `com.audioflow.app` for consistent Accessibility trust |

---

*Story created by @pm (Morgan)*
