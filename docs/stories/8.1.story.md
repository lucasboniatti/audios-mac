---
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools:
  - token_build_validation
  - source_of_truth_review
  - design_contract_validation
---
# Story 8.1: Formalizar design tokens compartilhados

**Epic:** EPIC-003 (AudioFlow Design System)  
**Status:** Ready for Review  
**Priority:** COULD  
**Effort:** M (Medium)  
**Phase:** 8 - Design System & Shared UI Foundations  

---

## Story

**Como** mantenedor do produto  
**Eu quero** consolidar os design tokens compartilhados como base visual única do AudioFlow  
**Para que** o companion web e futuras superfícies reutilizem a mesma linguagem visual sem drift manual  

---

## Acceptance Criteria

- [x] `design-tokens/tokens.yaml` permanece como source of truth dos tokens compartilhados
- [x] `scripts/build-tokens.js` gera artefatos válidos em CSS, JSON, Tailwind e Swift a partir de `tokens.yaml`
- [x] `frontend/styles.css` e a suíte local de qualidade continuam consumindo os tokens sem reintroduzir estilos hardcoded críticos
- [x] A política de arquivos `*.generated.*` fica explícita para evitar edição manual e drift entre source e outputs

---

## CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI não está habilitado em `.aiox-core/core-config.yaml`.
> A validação desta story seguirá revisão manual.

---

## Tasks / Subtasks

- [x] Revisar [design-tokens/tokens.yaml](/Users/lucasboniatti/Desktop/Projetos/audios-mac/design-tokens/tokens.yaml) contra `FR-014`, `AC-015` e `CON-008`
- [x] Consolidar [scripts/build-tokens.js](/Users/lucasboniatti/Desktop/Projetos/audios-mac/scripts/build-tokens.js) para gerar os artefatos compartilhados esperados pelo repo
- [x] Garantir que [frontend/styles.css](/Users/lucasboniatti/Desktop/Projetos/audios-mac/frontend/styles.css) e [tests/frontend-quality.test.mjs](/Users/lucasboniatti/Desktop/Projetos/audios-mac/tests/frontend-quality.test.mjs) permaneçam alinhados ao contrato dos tokens
- [x] Documentar a regra de source of truth e o caráter regenerável dos arquivos `*.generated.*` nos artefatos relevantes

---

## Dev Notes

### Contexto Atual

- A arquitetura já formaliza `design-tokens/` como fonte do design system e lista `tokens.yaml`, `tokens.json`, `tokens.css`, `tokens.tailwind.js` e `tokens.dtcg.json` como outputs esperados [ARCHITECTURE.md](/Users/lucasboniatti/Desktop/Projetos/audios-mac/docs/architecture/ARCHITECTURE.md#L102).
- O PRD agora adiciona `FR-014`, `AC-015` e `CON-008`, exigindo source of truth único e distinguindo artefatos gerados de arquivos editáveis [requirements.json](/Users/lucasboniatti/Desktop/Projetos/audios-mac/docs/prd/requirements.json).
- A story `7.3` já endureceu os quality gates do repo e alinhou o build de tokens ao schema atual, então esta story deve partir desse estado em vez de recriar o pipeline [7.3.story.md](/Users/lucasboniatti/Desktop/Projetos/audios-mac/docs/stories/7.3.story.md).

### Restrições

- `tokens.yaml` deve continuar sendo a única fonte editável do design system; outputs derivados não devem virar origem de verdade.
- O companion web já consome CSS tokenizado e não pode regredir visualmente durante essa consolidação.
- Esta story formaliza a base compartilhada; não inclui migração total do app macOS para componentes tokenizados.

### Suggested Files

- [design-tokens/tokens.yaml](/Users/lucasboniatti/Desktop/Projetos/audios-mac/design-tokens/tokens.yaml)
- [scripts/build-tokens.js](/Users/lucasboniatti/Desktop/Projetos/audios-mac/scripts/build-tokens.js)
- [frontend/styles.css](/Users/lucasboniatti/Desktop/Projetos/audios-mac/frontend/styles.css)
- [tests/frontend-quality.test.mjs](/Users/lucasboniatti/Desktop/Projetos/audios-mac/tests/frontend-quality.test.mjs)
- [.gitignore](/Users/lucasboniatti/Desktop/Projetos/audios-mac/.gitignore)

---

## Testing

- Rodar `node scripts/build-tokens.js` e verificar geração de artefatos válidos
- Rodar `npm run lint`
- Rodar `npm run typecheck`
- Rodar `npm test`
- Validar que o frontend continua importando tokens sem fallback manual para valores hardcoded

---

## Change Log

- 2026-03-14: Story criada a partir de `EPIC-003` para formalizar a base de tokens já presente no repo
- 2026-03-14: `tokens.yaml` passou a declarar explicitamente o source of truth e ganhou aliases semânticos faltantes para os contratos do frontend
- 2026-03-14: `build-tokens.js` foi consolidado para gerar `tokens.json`, `tokens.dtcg.json`, `tokens.css`, `tokens.generated.css`, `tokens.tailwind.js`, `tokens.tailwind.generated.js` e `AudioFlowColors.generated.swift`
- 2026-03-14: A suíte `frontend-quality` passou a validar os artefatos canônicos e a equivalência com os arquivos `*.generated.*`
- 2026-03-14: Fixes de QA aplicados para sincronizar o output Swift com `AudioFlow/Sources/DesignTokens/AudioFlowColors.swift` e amarrar o build do frontend ao `tokens.tailwind.js` gerado

---

## File List

| File | Action |
|------|--------|
| `design-tokens/tokens.yaml` | Modified - documented the editable source of truth and completed semantic aliases consumed by the shared token contract |
| `scripts/build-tokens.js` | Modified - now generates the canonical CSS, DTCG JSON, Tailwind and Swift artifacts from `tokens.yaml`, mirrors the Swift output into `AudioFlow/Sources/DesignTokens/AudioFlowColors.swift` and keeps `*.generated.*` outputs aligned |
| `frontend/styles.css` | Modified - still imports `../design-tokens/tokens.css` and now declares the frontend Tailwind config explicitly in the CSS build entrypoint |
| `frontend/tailwind.config.js` | Modified - now extends the generated `../design-tokens/tokens.tailwind.js` instead of duplicating the token contract manually |
| `AudioFlow/Sources/DesignTokens/AudioFlowColors.swift` | Modified - now stays synchronized with the generated Swift color artifact from `tokens.yaml` |
| `tests/frontend-quality.test.mjs` | Modified - now asserts canonical token artifacts, source-of-truth messaging, Tailwind consumption and Swift parity with the generated compatibility outputs |
| `.gitignore` | Modified - clarifies that `*.generated.*` token artifacts are regenerable while canonical shared outputs remain explicit |
| `docs/stories/8.1.story.md` | Modified - tracked implementation progress, validation and completion notes for Story 8.1 |

---

## Dev Agent Record

### Agent Model Used

- GPT-5 Codex

### Debug Log References

- `node scripts/build-tokens.js`
- `npm run lint`
- `npm run typecheck`
- `npm test`
- `npm run build`
- `swift test`

### Completion Notes List

- `design-tokens/tokens.yaml` agora declara explicitamente o source of truth e cobre aliases semânticos faltantes para os estados e superfícies usados pelo frontend
- `scripts/build-tokens.js` passou a gerar os artefatos canônicos consumidos pelo repo (`tokens.css`, `tokens.tailwind.js`, `tokens.dtcg.json`) e os compatíveis `*.generated.*` a partir da mesma origem
- A suíte local de qualidade passou a validar a política de geração e a equivalência entre os artefatos canônicos e os compatíveis
- `frontend/styles.css` continuou alinhado ao contrato dos tokens ao importar `../design-tokens/tokens.css` sem reintroduzir valores críticos hardcoded
- O build de tokens agora sincroniza o arquivo Swift efetivamente compilado em `AudioFlow/Sources/DesignTokens/AudioFlowColors.swift`, reduzindo drift entre o design system e o app macOS
- `frontend/tailwind.config.js` passou a estender `../design-tokens/tokens.tailwind.js`, e `frontend/styles.css` declara esse config explicitamente no entrypoint do PostCSS/Tailwind
- `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` e `swift test` passaram apos os fixes de QA

---

*Story created by @sm (River)*

## QA Results

### Review Date

- 2026-03-14 (re-review)

### Reviewed By

- Quinn (Test Architect)

### Findings

- [x] O build de tokens agora sincroniza [AudioFlowColors.generated.swift](/Users/lucasboniatti/Desktop/Projetos/audios-mac/design-tokens/AudioFlowColors.generated.swift#L1) com [AudioFlowColors.swift](/Users/lucasboniatti/Desktop/Projetos/audios-mac/AudioFlow/Sources/DesignTokens/AudioFlowColors.swift#L1), removendo o drift entre o design system e o consumidor Swift compilado pelo app.
- [x] O frontend passou a estender [tokens.tailwind.js](/Users/lucasboniatti/Desktop/Projetos/audios-mac/design-tokens/tokens.tailwind.js#L1) em [tailwind.config.js](/Users/lucasboniatti/Desktop/Projetos/audios-mac/frontend/tailwind.config.js#L1), e o entrypoint [styles.css](/Users/lucasboniatti/Desktop/Projetos/audios-mac/frontend/styles.css#L2) declara esse config explicitamente no build CSS.
- [x] Nenhum novo finding bloqueante foi identificado nesta reavaliacao estatica.

### Security Review

- PASS: a story continua restrita ao pipeline local de tokens e nao introduz nova superficie de ataque observavel.

### Performance Considerations

- PASS: `node scripts/build-tokens.js`, `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` e `swift test` passaram sem sinal de regressao relevante de tempo.

### Files Modified During Review

- [docs/stories/8.1.story.md](/Users/lucasboniatti/Desktop/Projetos/audios-mac/docs/stories/8.1.story.md)
- [8.1-formalizar-design-tokens-compartilhados.yml](/Users/lucasboniatti/Desktop/Projetos/audios-mac/docs/qa/gates/8.1-formalizar-design-tokens-compartilhados.yml)

### Gate Status

- Gate: PASS -> [8.1-formalizar-design-tokens-compartilhados.yml](/Users/lucasboniatti/Desktop/Projetos/audios-mac/docs/qa/gates/8.1-formalizar-design-tokens-compartilhados.yml)

### Recommended Status

- [✓ Ready for Done]
