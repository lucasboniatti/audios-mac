# Story 2.1: Implementar menu bar status item

**Epic:** EPIC-001 (AudioFlow)  
**Status:** Draft  
**Priority:** MUST  
**Effort:** S (Small)  
**Phase:** 2 - Menu Bar App Base  

---

## Story

**Como** usuário  
**Eu quero** ver o ícone do AudioFlow no menu bar  
**Para que** eu saiba que o app está rodando e possa interagir com ele  

---

## Acceptance Criteria

- [ ] Ícone de microfone aparece no menu bar ao iniciar o app
- [ ] Menu dropdown abre ao clicar no ícone
- [ ] Menu tem opção "Sair" que fecha o app
- [ ] Menu tem opção "Sobre AudioFlow" com versão
- [ ] App não aparece no Dock (LSUIElement já configurado)

---

## Technical Details

### Menu Structure

```
🎤 AudioFlow
├── Sobre AudioFlow
├── ───────────────
└── Sair
```

### Files to Update/Create

- `AppDelegate.swift` (atualizar)
- `Views/MenuBuilder.swift` (novo)

### Key APIs

- `NSStatusBar.system`
- `NSStatusItem`
- `NSMenu`
- `NSImage(systemSymbolName:)`

### AppDelegate Implementation

```swift
import Cocoa

@main
class AppDelegate: NSObject, NSApplicationDelegate {
    var statusItem: NSStatusItem?
    
    func applicationDidFinishLaunching(_ aNotification: Notification) {
        setupStatusBar()
    }
    
    func setupStatusBar() {
        statusItem = NSStatusBar.system.statusItem(withLength: NSStatusItem.squareLength)
        
        if let button = statusItem?.button {
            button.image = NSImage(systemSymbolName: "mic.fill", accessibilityDescription: "AudioFlow")
        }
        
        let menu = MenuBuilder.buildMenu()
        statusItem?.menu = menu
    }
}
```

### MenuBuilder.swift

```swift
import Cocoa

class MenuBuilder {
    static func buildMenu() -> NSMenu {
        let menu = NSMenu()
        
        let aboutItem = NSMenuItem(title: "Sobre AudioFlow", action: #selector(showAbout), keyEquivalent: "")
        aboutItem.target = MenuBuilder.self
        menu.addItem(aboutItem)
        
        menu.addItem(NSMenuItem.separator())
        
        let quitItem = NSMenuItem(title: "Sair", action: #selector(NSApplication.terminate(_:)), keyEquivalent: "q")
        menu.addItem(quitItem)
        
        return menu
    }
    
    @objc static func showAbout() {
        NSApp.activate(ignoringOtherApps: true)
        NSApp.orderFrontStandardAboutPanel(nil)
    }
}
```

---

## Dev Notes

- Ícone SF Symbol: `mic.fill`
- Menu será expandido nas próximas stories
- NÃO usar SwiftUI para menu bar (AppKit puro)

---

## Definition of Done

- [ ] App inicia e mostra ícone no menu bar
- [ ] Clique abre menu
- [ ] "Sair" fecha o app
- [ ] "Sobre" mostra dialog padrão do macOS

---

*Story created by @pm (Morgan)*
